import React, { Component } from "react";
import { View, Text } from "react-native";

export default class ExistingTrain extends Component{
    render() {
        return(
        <View>
            <Text>This is existing train</Text>
        </View>);
    }
}